# Agent server
fastapi
uvicorn
pydantic
pyzmq
mujoco>=3.3.0
Pillow
numpy

# Robocasa & Robosuite (install via their setup.py, but these are the key deps)
# pip install -e robosuite/ && pip install -e robocasa/
