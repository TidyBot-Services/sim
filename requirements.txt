# Sim server + bridges
mujoco>=3.3.0
numpy
Pillow
pyzmq
msgpack
websockets

# Camera server
httpx

# Robocasa & Robosuite (installed editable by setup.sh)
# pip install -e robosuite/ && pip install -e robocasa/
